const createTodo = (title, description, date, priority) => {
    const setDate = (date) => this.date; 
    const setPriority = (priority) => this.priority; 
    return {title, description, date, priority, setDate, setPriority}; 
};